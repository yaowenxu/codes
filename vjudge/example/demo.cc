#include <cstring>
#include <cstdio>
#include <iostream>

// 编译命令：c++ demo.cc -o demo ; ./demo ; rm demo

using namespace std;

int main(){
    freopen("demo.in", "r", stdin); // 重定向标准输入输出；
    string in;
    cin >> in;
    cout << in << endl;
    cout << "vjudge!" << endl;
    return 0;
}